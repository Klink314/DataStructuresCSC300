#include "A0Klinkenborg.h"

int main()
{
    node *head = nullptr;
    node *myNode = nullptr;

    int choice, data, index;

    do{

    }while(choice != 5);

    cout << "Code ran" << endl;

    return 0;
}