#include "A0Klinkenborg.h"

int main()
{
    node *head = nullptr;
    node *myNode = nullptr;

    int choice, data, index;

    cout << "Enter choice between 1-4: ";
    cin >> choice;

    do{

    }while(choice != 5);

    cout << "Code ran" << endl;

    return 0;
}